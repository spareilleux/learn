<Project Sdk="Microsoft.NET.Sdk">

  <!-- The small methods the lessons disassemble with ilspycmd: kept apart so that their IL doesn't move -->
  <PropertyGroup>
    <Nullable>disable</Nullable>
  </PropertyGroup>

</Project>
