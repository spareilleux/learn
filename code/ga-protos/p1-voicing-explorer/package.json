{
  "name": "ga-p1-voicing-explorer",
  "private": true,
  "version": "0.0.0",
  "description": "Guitar Alchemist Lab, prototype 1: a 3D explorer of GA's OPTIC-K voicing space. A deterministic build step (PCA, sample, exact neighbours) turns GA's optick.index into a small binary file, and a three.js WebGPU page draws it, shows each voicing's diagram and neighbours, and plays it",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test \"tests/*.test.mjs\"",
    "fixture": "node scripts/make-fixture.mjs",
    "data": "node scripts/build-data.mjs",
    "measure": "node scripts/measure.mjs",
    "publish-site": "node scripts/publish.mjs"
  },
  "dependencies": {
    "three": "0.186.0"
  },
  "devDependencies": {
    "playwright": "1.63.0",
    "vite": "8.3.0"
  }
}
