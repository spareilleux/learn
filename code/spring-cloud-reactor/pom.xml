<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Spring Boot's parent POM: plugin versions, dependency versions (its BOM) and sensible defaults -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>io.github.spareilleux.learn</groupId>
  <artifactId>spring-cloud-reactor</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Spring Boot, Spring Cloud and Reactor for C# developers: course examples</name>

  <modules>
    <module>music</module>
    <module>l01-mvc</module>
    <module>l02-reactor</module>
    <module>l03-reactor</module>
    <module>l04-webflux</module>
    <module>l05-r2dbc</module>
    <module>l06-gateway</module>
  </modules>

  <properties>
    <java.version>25</java.version>
    <!-- The release train that start.spring.io pairs with Spring Boot 4.1.x -->
    <spring-cloud.version>2025.1.3</spring-cloud.version>
    <blockhound.version>1.0.17.RELEASE</blockhound.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.github.spareilleux.learn</groupId>
        <artifactId>music</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>io.projectreactor.tools</groupId>
        <artifactId>blockhound</artifactId>
        <version>${blockhound.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <compilerArgs>
              <arg>-Xlint:all,-processing</arg>
            </compilerArgs>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
