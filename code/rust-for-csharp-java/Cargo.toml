[package]
name = "rust-for-csharp-java"
version = "0.1.0"
edition = "2024"
publish = false
description = "Runnable examples and compile-fail checks for the 'Rust for C#/Java developers' course"

[dependencies]

[dev-dependencies]
rayon = "1.12.0"
tokio = { version = "1.53.1", features = ["rt-multi-thread", "macros", "time", "sync"] }
